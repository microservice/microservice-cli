version: 2
jobs:
  build:
    docker:
      - image: circleci/node:9.7
    steps:
      - checkout
      - run:
          name: yarn-install
          command: yarn install
      - run:
          name: eslint
          command: yarn eslint
      - run:
          name: test
          command: yarn test
